import React, { createContext } from 'react';

const counterContext = createContext(0);

export default counterContext;












// create context  ====>> we will create an api for to transfer the data to the components


// provider ===>> through the provider we will provide the data to the component


// consumer ====>> through the consumer we will consume the context 









